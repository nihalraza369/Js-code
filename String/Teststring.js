//Q:01  Generate a username  for them  based  on the  input .start username with @,followed  by their full name and ending with the full name lenght?

//lets start
let mark="@"
let username="nihalraza"
let res=mark.concat(username)
console.log(res,res.length);

   //or
   
let mark1="@"
let username1="nihalraza"
let res1=mark.concat(username1)
let res2=`${res1}${res1.length}`
console.log(res2);

